      
      // login reute.ts 
      // want to show user more info on who is login
      const response = NextResponse.json({
      message: "Login successful",
      token: token,
    });

     //need to pass more date for users login info
     //this now shows a user more date on who is login in to page

      const response = NextResponse.json({
      message: "Login successful",
      token: token,
      email: user.email,
      username: user.username,
      createdAt: user.createdAt,
    });

 
    // UserContext : what data is need ?
    // i want to add a user to UserState

      interface UserState {
      token: string | null;
      setToken: (token: string) => void;
     }
